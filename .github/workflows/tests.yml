name: Tests
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  cpu-tests:
    runs-on: ubuntu-24.04
    env:
      HF_HUB_OFFLINE: "1"
      TRANSFORMERS_OFFLINE: "1"
      USE_TF: "0"
      OMP_NUM_THREADS: "2"
      MKL_NUM_THREADS: "2"
      OPENBLAS_NUM_THREADS: "2"
      MPLBACKEND: Agg
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-python@v7.0.0
        with:
          python-version: "3.12"
          cache: pip
      - name: Install
        run: python -m pip install -e .
      - name: Test
        run: python -m pytest -q
      - name: Validate complete model pipelines
        run: python -m scripts.smoke --output-dir runs/ci-smoke
      - name: Export manuscript tables
        run: python -m scripts.export_paper_results --output-dir runs/paper-tables
      - name: Plot manuscript results
        run: python -m scripts.plot_paper_results --output-dir runs/paper-figures
